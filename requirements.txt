# --- needed for the mock run (Stage 4-7 core) ---
networkx>=3.0

# --- extraction (Stage 1-2): wire up when adding Claude ---
anthropic>=0.39.0
pydantic>=2.0

# --- canonicalization (Stage 3) ---
rapidfuzz>=3.0
sentence-transformers>=2.2
scikit-learn>=1.3
hdbscan>=0.8.33

# --- DSL prompt audit/verify (src/dsl_verify.py token budget check) ---
tiktoken>=0.7

# --- analysis (Stage 5, optional process-mining upgrade) ---
pm4py>=2.7

# --- visualization (Stage 6) ---
graphviz>=0.20      # also needs the `dot` binary: `brew install graphviz`
pydot>=1.4
plotly>=5.0
pyvis>=0.3

# --- speech-to-text (mp3 -> transcript): all LOCAL, no API ---
# Also requires the system `ffmpeg` binary: `brew install ffmpeg` (mac) / `apt install ffmpeg`.
# First run downloads ~3-4GB of models (Whisper large-v3, pyannote, wav2vec align, MiniLM).
# Needs a (free) HuggingFace token + accepted gated models:
#   huggingface-cli login  AND accept  pyannote/speaker-diarization-3.1 + pyannote/segmentation-3.0
whisperx>=3.7
faster-whisper>=1.2
pyannote.audio>=3.4
speechbrain==1.0.3   # PINNED: 1.1.0 breaks on macs (eager k2 import). Do not bump blindly.
ctranslate2>=4.8
onnxruntime>=1.19
soundfile>=0.13
