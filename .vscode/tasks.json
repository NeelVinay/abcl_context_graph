{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "1. Load transcripts (from a folder)",
      "detail": "Pulls transcripts from a folder anywhere on disk into intake/transcripts/ and registers them. Does not build the graph or touch the prompt.",
      "type": "shell",
      "command": "./bin/load-transcripts '${input:transcriptsPath}'",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "1b. Re-register intake/transcripts/ (no pull)",
      "detail": "Registers whatever is already sitting in intake/transcripts/, without pulling from anywhere else.",
      "type": "shell",
      "command": "./bin/load-transcripts",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "2. Load prompt (from a file)",
      "detail": "Pulls a prompt file anywhere on disk into intake/prompt/, validates it, and confirms its transcripts are loaded.",
      "type": "shell",
      "command": "./bin/load-prompt '${input:promptPath}'",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "3. Run pipeline (full run)",
      "detail": "Context graph -> analysis -> improved prompt, start to finish. Uses the local Claude Code CLI — no API key needed. Writes deliverables/.",
      "type": "shell",
      "command": "./bin/run-pipeline",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true,
        "focus": true
      },
      "problemMatcher": [],
      "group": { "kind": "build", "isDefault": true }
    },
    {
      "label": "Advanced: improve a prompt file directly (bypasses intake/deliverables)",
      "detail": "Testing tool. Improves any prompt file without touching intake/ or the real deliverables/ — writes to platform/test-output/ instead.",
      "type": "shell",
      "command": "./platform/improve '${input:advancedPromptPath}'",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "Advanced: reset a client to zero",
      "detail": "Wipes a client's loaded transcripts/cache/output back to zero (archived, never deleted) — for proving run-pipeline regenerates everything from scratch.",
      "type": "shell",
      "command": "./platform/reset '${input:resetClient}'",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "Show last diff",
      "detail": "The before/after patch from the most recent ./bin/run-pipeline run.",
      "type": "shell",
      "command": "cat deliverables/Changes.diff",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    }
  ],
  "inputs": [
    {
      "id": "transcriptsPath",
      "type": "promptString",
      "description": "Folder of transcripts to load (leave as-is to just re-register intake/transcripts/)",
      "default": "~/Downloads"
    },
    {
      "id": "promptPath",
      "type": "promptString",
      "description": "Path to the prompt file to load",
      "default": "~/Downloads"
    },
    {
      "id": "advancedPromptPath",
      "type": "promptString",
      "description": "Path to any prompt file to test-improve directly",
      "default": "intake/prompt/"
    },
    {
      "id": "resetClient",
      "type": "promptString",
      "description": "Client key to reset to zero",
      "default": "abcl"
    }
  ]
}
